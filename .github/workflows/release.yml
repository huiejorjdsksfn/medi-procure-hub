# This workflow is retired — all builds handled by procurbosse.yml
# Kept as empty stub to avoid 404s on old run links
name: "Release (retired)"
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use procurbosse.yml workflow for builds and releases."

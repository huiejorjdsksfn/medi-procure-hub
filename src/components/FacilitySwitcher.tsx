/**
 * FacilitySwitcher -- DEPRECATED v8.0
 * Replaced by the EL5 system status pill in AppLayout.
 * FacilityProvider removed -- all 10 enterprise APIs handle multi-entity access.
 * Kept as a stub to avoid any lingering import errors.
 */
export default function FacilitySwitcher() {
  return null;
}

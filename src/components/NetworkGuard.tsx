/**
 * ProcurBosse — NetworkGuard v3.0 NUCLEAR
 * IP restriction DISABLED — passes all traffic through
 * IP access control is available in Admin → IP Access page (opt-in only)
 * EL5 MediProcure · Embu Level 5 Hospital
 */
export default function NetworkGuard({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

import type React from "react";
